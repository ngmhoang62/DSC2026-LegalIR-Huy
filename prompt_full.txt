<USER_REQUEST>
You are executing the next major experiment on folder sota. This is an EXECUTION task.

The highest priority is:

1. actual retrieval performance;
2. generalization to public test;
3. truthful, auditable execution.

Do not stop at implementation.

Do not substitute a proxy model.

Do not report neural fine-tuning unless transformer parameters actually received gradients and changed.

---

# 0. AUTHORITATIVE BASELINE

Endpoint:

`profile_memory_plus_sparse_rank_scores`

Strict 5-fold OOF:

* Recall@5 = `0.9488556715777428`
* Precision@5 = `0.20314690316120732`
* Single-gold Recall@5 = `0.9641304347826087`
* Multi-gold Recall@5 = `0.7703266787658803`

Per fold:

* fold_0 = `0.9524320457796852`
* fold_1 = `0.9451597520267048`
* fold_2 = `0.9511555873242793`
* fold_3 = `0.9420243204577969`
* fold_4 = `0.9535050071530758`

Current Top-8:

`0.9631359366804939`

Candidate pool ceiling:

`0.9819315310160681`

Current public leaderboard from the authoritative candidate:

* Recall = `0.946166668`
* Precision = `0.204200001`

Do not overwrite this submission.

---

# 1. VERSION CONTROL FIRST

Current GitHub remote HEAD was last observed as:

`3e5811c627ea24b557e9f9c4b4cef282c06945eb`

The previous sparse experiment reported:

`?? src/gemini/huy_sparse_resurrection_v1/`

meaning its new source namespace was untracked at execution time.

Before this experiment:

1. run `git rev-parse HEAD`;
2. run `git status --porcelain`;
3. record the exact starting HEAD and working-tree state in the experiment manifest;
4. verify that the previously completed `src/gemini/huy_sparse_resurrection_v1/`
   namespace is already tracked in Git;
5. do NOT modify previous experiment namespaces or upstream pipelines;
6. do NOT commit giant checkpoints, SQLite databases, score caches, or JSONL
   prediction caches unless repository policy explicitly requires them.

Expected starting HEAD at the time this prompt was written:

`2504c45d66c3a75c59be1e9a6ea706aa7398bee0`

Do not hard-code this SHA into experiment logic.
If HEAD has advanced normally, record the actual new HEAD and continue.

Prefer starting the experiment from a clean working tree.
If the tree is already dirty before creating the new experiment namespace,
record exactly which files are dirty and do not silently include unrelated changes.

Record actual state.

Never hard-code a Git SHA in experiment code.

---

# 2. NEW NAMESPACE

Create:

`src/gemini/jina_honest_adaptation_v1/`

Results:

`results/gemini/jina_honest_adaptation_v1/`

All prior namespaces and authoritative fasttrack files are read-only.

---

# 3. CORRECT INTERPRETATION OF 5-FOLD VS FULL-DATA TRAINING

Do NOT treat 5-fold checkpoints as the intended public deployment.

5-fold exists to estimate generalization.

For supervised models:

* strict OOF model for fold F trains only without F;
* final public model may train on all 6,991 evaluable queries after the recipe has been selected.

The current adapted E5 and final LR already use this pattern.

This experiment must follow the same philosophy for Jina.

We specifically want to measure whether more training data improves Jina:

approximately:

* ~4.2k training queries;
* ~5.6k training queries;
* 6,991 final deployment queries.

The 6,991 model must NEVER be reported as an OOF score.

---

# 4. HYPOTHESIS

The current strongest endpoint uses a frozen:

`jinaai/jina-reranker-v2-base-multilingual`

over Huy's proven lexical evidence interface:

`top_passages(question, parent, count=2, window=220, overlap=70)`

This frozen CE is one of the strongest components in the endpoint.

Historical task-finetuned Jina evidence suggests supervised adaptation may be valuable.

However prior adaptation attempts are NOT decisive:

1. old Jina pairwise/LoRA emphasized extremely hard topical negatives and performed poorly;
2. `provision_reranker_v1` did NOT actually train Jina at all.

New hypothesis:

> Genuine parent-level Jina fine-tuning with balanced semi-hard negative sampling, Huy's exact locked evidence renderer, multi-positive listwise learning, and controlled stability regularization can create a stronger and complementary CE expert.

---

# 5. DO NOT CHANGE THE EVIDENCE INTERFACE YET

Use ONLY:

`R0_HUY_LOCKED`

Exact function/semantics currently used by authoritative frozen Jina.

For every q-parent:

* exactly top 2 Huy lexical passages;
* same window;
* same overlap;
* same header behavior;
* same max_length contract.

Do NOT use:

* R1;
* R2;
* structural_v3;
* provision experiment evidence bank.

Those representations were either not honestly tested or previously degraded ranking.

This experiment isolates task adaptation.

---

# 6. PRE-TRAINING PARITY

Recompute frozen Jina scores on a deterministic audit sample.

Compare against authoritative cache.

Required:

* at least 200 query-parent pairs;
* numeric tolerance documented;
* parent ranking parity;
* passage-text hash parity.

Write:

`FROZEN_JINA_PARITY.json`

Do not train until parity passes.

---

# 7. REAL TRANSFORMER TRAINING PROOF

This is mandatory.

Before training record:

* model class;
* original parameter count;
* trainable parameter count;
* SHA256/state fingerprint of selected trainable tensors;
* classifier-head fingerprint;
* frozen-model deterministic logits on 256 audit pairs.

During training record:

* actual CUDA device;
* optimizer class;
* learning rate;
* optimizer step count;
* gradient norm each N steps;
* non-zero gradient fraction;
* training loss;
* validation loss;
* GPU peak memory.

After training record:

* trainable tensor fingerprint;
* number of tensors changed;
* L2 parameter delta;
* audit-pair adapted logits;
* mean absolute logit change;
* checkpoint SHA256.

Requirements:

* optimizer steps > 0;
* gradient norm must be non-zero;
* trainable parameter tensors must differ before/after.

If not, status is:

`NEURAL_TRAINING_NOT_EXECUTED`

and the experiment must not call itself adapted Jina.

Write:

`NEURAL_TRAINING_PROOF.json`

No sklearn model may substitute for Jina training.

---

# 8. TRAINING CANDIDATE GROUPS

Use the authoritative 51-document candidate pool/ranking.

For each training query:

ALL available canonical gold parents in pool are positives.

Never treat another gold parent as negative.

Build negative pool from authoritative final ranking, not just frozen Jina.

Primary curriculum:

## Semi-hard

non-gold ranks 6–20

sample 4

## Medium

non-gold ranks 21–40

sample 3

## Tail

remaining pool

sample 1

Do NOT make top-1 hardest non-gold the dominant training negative.

---

# 9. H_LOCAL DIVERSITY NEGATIVE

The previous sparse resurrection discovered a complementary local sparse retriever.

Use it ONLY as a negative-diversity source.

For each query, if available:

select at most ONE additional non-gold parent that:

* ranks high under H_LOCAL;
* is not already in the selected negatives;
* is canonical;
* is not a sibling gold.

This is intended to expose lexical legal confusers different from final-LR confusers.

Do not add H_LOCAL as a production feature in this experiment.

---

# 10. TWO-STAGE CURRICULUM

Train for 2 epochs initially.

### Epoch 1

Use only:

* semi-hard;
* medium;
* tail;
* optional H_LOCAL diversity negative.

NO authoritative Top-5 negative.

### Epoch 2

For each query optionally add ONE non-gold from authoritative ranks 1–8.

This hard negative is allowed only in epoch 2.

Do not add multiple hardest negatives.

This curriculum tests whether the model can first learn robust relevance before seeing fragile boundary confusers.

---

# 11. PARENT-LEVEL MULTI-INSTANCE SCORING

Each parent has 2 Huy passages.

Run Jina on both query-passage pairs.

Do not label both passages independently positive/negative.

Aggregate the two passage logits into a parent score using smooth maximum:

`parent_score = tau_pool * logsumexp(passage_scores / tau_pool)`

Primary:

`tau_pool = 0.15`

If raw Jina logit scale makes this numerically inappropriate, choose a fixed scale using INNER-TRAIN statistics only and document why.

Do not use outer-held labels to tune pooling.

Frozen baseline parent score remains max for parity comparison.

---

# 12. MULTI-POSITIVE GROUP OBJECTIVE

For query q with candidate parent set C and gold-positive subset P:

primary ranking loss:

`L_rank = -log( sum_{p in P} exp(s_p/T) / sum_{d in C} exp(s_d/T) )`

Use:

`T = 1.0`

Every query receives equal weight.

All gold parents contribute jointly.

This directly supports multi-gold queries.

---

# 13. STABILITY REGULARIZATION

Cache frozen-Jina parent scores for every training group.

Add a modest stability term:

`L = L_rank + lambda_anchor * L_anchor`

where the anchor penalizes excessive change from frozen-Jina score geometry.

Evaluate only:

`lambda_anchor ∈ {0.05, 0.15}`

Do not repeat the previous fake alpha=0.5 proxy.

Selection must occur on inner validation only.

---

# 14. FULL FINETUNE VS LoRA — SMALL INNER SCREEN

Do not blindly assume LoRA is best.

On outer fold 0 TRAINING POPULATION ONLY:

create deterministic inner train/validation split.

Compare exactly:

### M1 — LoRA

Conservative attention LoRA.

### M2 — Full fine-tune

All Jina parameters trainable.

Use memory-saving methods as necessary:

* bf16/fp16;
* gradient checkpointing;
* small microbatch;
* gradient accumulation.

Do not evaluate these choices on outer fold-0 labels.

Choose M1 or M2 solely by inner validation Recall@5 and ranking loss.

Then LOCK the training method for all 5 outer folds.

Write:

`FINETUNE_METHOD_SELECTION.json`

---

# 15. LEARNING RATE SCREEN

Inside the same inner selection only:

for selected method test:

`lr ∈ {5e-6, 1e-5, 2e-5}`

and:

`lambda_anchor ∈ {0.05, 0.15}`

At most 6 combinations.

Do not perform another giant sweep.

Freeze the selected recipe before outer evaluation.

---

# 16. STRICT 5-FOLD OOF

For each outer fold F:

Training population:

all evaluable queries outside F
minus duplicate-linked exclusions.

Approximately 5.6k queries.

Held F labels must be invisible to:

* optimization;
* early stopping;
* negative mining policy selection;
* hyperparameter selection;
* checkpoint selection;
* calibration.

Train one adapted Jina model.

Score the authoritative candidate pool for F.

Persist raw passage logits and parent scores.

Write one immutable prediction/checkpoint manifest per fold.

---

# 17. TRAINING-SIZE SCALING STUDY

This directly tests the user's hypothesis that 5-fold training may underuse available labels.

For EACH outer fold, in addition to the main ~5.6k fit:

create TWO deterministic stratified 75% subsets of its outer-training population.

These should each contain approximately 4.2k queries.

Train the exact locked Jina recipe on each subset.

Evaluate both on the SAME outer-held fold.

Average the two ~4.2k held scores.

Compare against the main ~5.6k model.

Report:

* R@5;
* standalone adapted Jina;
* final Huy integration;
* single/multi;
* per-fold.

Estimate:

`gain_4.2k_to_5.6k`

Do NOT claim an exact 7k OOF extrapolation.

But report a simple descriptive slope.

Write:

`TRAIN_SIZE_SCALING_REPORT.json`

This is the empirical answer to whether additional training data helps.

---

# 18. SEED ENSEMBLE TEST — CLEAN VERSION

Do NOT ensemble the five ordinary fold models on their own held queries, because four of those models may have seen the held labels.

Instead, for each outer fold:

train exactly 3 models on the SAME clean outer-training population using seeds:

`2026, 3407, 7919`

All three exclude the held fold.

Score held fold.

Compare:

### E_SINGLE

seed 2026.

### E_3SEED

mean standardized parent score across the 3 adapted Jina models.

This gives a valid estimate of variance-reduction benefit.

Report compute cost.

If E_3SEED gain < `+0.0004` final endpoint, do not use a public seed ensemble.

---

# 19. STANDALONE JINA METRICS

For:

* frozen Jina;
* adapted single;
* adapted 3-seed if executed;

report:

* R@1
* R@5
* R@8
* R@10
* single/multi;
* per fold;
* wins/losses vs frozen;
* gold crossing counts.

Also report Top-5 union oracle with the authoritative endpoint.

Write:

`JINA_OOF_REPORT.json`

---

# 20. INTEGRATE INTO AUTHORITATIVE 0.948855 ENDPOINT

Keep every existing feature/channel unchanged.

Evaluate:

## J0

authoritative endpoint.

## J1_REPLACE

replace frozen Jina CE rank/score with adapted Jina.

## J2_AUGMENT

keep frozen Jina AND add adapted Jina rank/score.

## J3_RESIDUAL

J2 plus:

* adapted_z - frozen_z;
* adapted_rank - frozen_rank.

No other new features.

## J4_ENSEMBLE

Only if clean 3-seed CE ensemble is useful:
use ensemble-adapted rank/score instead of single adapted channel.

Same:

* StandardScaler;
* balanced LR;
* C=.15;
* liblinear.

No XGBoost.
No LightGBM.
No LambdaRank.
No boundary rules.

Write:

`FINAL_INTEGRATION_REPORT.json`

---

# 21. IMPORTANT MULTI-GOLD AUDIT

Previous experiments repeatedly harmed multi-gold retrieval.

For every adapted endpoint report separately:

* 6,440 single-gold queries;
* 551 multi-gold queries.

Also report:

* queries where baseline already has one gold in Top-4 and another gold is ranks 5–10;
* whether adapted Jina rescues or damages those cases.

Do not introduce query rules based on this audit.

---

# 22. BOUNDARY AUDIT

Use the already-defined rank5–8 opportunity concept only for evaluation.

Report:

* baseline opportunity count;
* adapted Jina preferred gold challenger;
* actual final endpoint wins;
* actual losses;
* defended rank5 golds evicted.

Do not use the old forensic QID list during training.

Recompute populations from predictions/gold.

---

# 23. GENERALIZATION SLICES

Report strict held performance by:

### Gold-label familiarity

* all gold parents seen somewhere in outer-training;
* at least one unseen gold parent.

### Question similarity

Using frozen LAL query embedding:

* high similarity to outer train;
* medium;
* low.

Thresholds based on training quantiles.

### Document length

training-derived quartiles.

### Frozen/adapted disagreement

* agree;
* adapted promotes;
* adapted demotes.

### Negative source

For errors/rescues:

* semi-hard;
* medium;
* tail;
* H_LOCAL diversity;
* hard curriculum candidate.

We want to know whether adaptation generalizes beyond repeatedly-seen labels.

Write:

`GENERALIZATION_AUDIT.json`

---

# 24. FULL-DATA FINAL JINA FIT

ONLY if strict OOF adapted Jina/integration is positive enough to justify deployment.

Promotion threshold to trigger full-data fit:

best final endpoint delta >= `+0.0007`

OR:

adapted Jina provides very strong complementary oracle evidence and no catastrophic fold.

Then train a SINGLE final Jina model on all:

`6,991`

evaluable train queries.

Same locked:

* evidence renderer;
* negatives;
* curriculum;
* pooling;
* objective;
* optimizer recipe;
* epochs.

No validation labels exist now.

Use the training duration/checkpoint rule frozen from OOF.

Write:

`FULL6991_TRAINING_MANIFEST.json`

State explicitly:

`reported_as_oof = false`

---

# 25. PUBLIC SINGLE VS ENSEMBLE POLICY

Default public deployment:

`FULL6991_SINGLE`

Do not use five fold models as public ensemble by default.

If clean 3-seed OOF ensemble gave >= `+0.0004` endpoint gain:

also train 3 full-data seeds and create:

`FULL6991_3SEED`

Otherwise train only one full-data model.

This directly separates:

* more training data;
* model variance ensemble.

---

# 26. PUBLIC MATERIALIZATION

If promoted:

score the exact current public candidate pool.

Keep all current public components unchanged.

Replace/augment only the Jina feature according to the winning strict-OOF integration arm.

Final LR still trains on all 6,991 training queries using:

* OOF adapted Jina scores for train rows;
* full-data Jina scores for public rows.

This is critical.

Do NOT train final LR using in-sample full-data Jina scores on train rows.

Training LR must see cross-fitted Jina scores matching the deployment feature semantics as closely as possible without target leakage.

For public:
use the full-6991 Jina checkpoint.

Create a NEW submission candidate.

Never overwrite:

`submission_candidate_with_huy_jina`

---

# 27. TRAIN/DEPLOY FEATURE-SHIFT AUDIT

Because train LR sees OOF Jina while public sees full-data Jina, quantify score shift.

Compare frozen vs adapted:

* mean;
* std;
* top1-top5 margins;
* within-query z-score distributions;
* rank churn.

Use normalization that does not require public labels.

If full-data score scale differs, apply only a label-free calibration derived from TRAIN OOF vs full-data training-query score distributions.

Do NOT calibrate from public leaderboard.

Write:

`TRAIN_DEPLOY_SHIFT_AUDIT.json`

---

# 28. ERROR CASEBOOK — CONDITIONAL

If best strict delta is:

`0 < delta < +0.002`

produce:

`JINA_ADAPTATION_CASEBOOK.jsonl`

Include:

* all endpoint wins;
* all endpoint losses;
* up to 30 remaining Top-8 opportunities.

Each record:

* query;
* gold IDs;
* baseline Top-10;
* adapted Top-10;
* frozen Jina logits;
* adapted passage logits;
* adapted parent score;
* exact two Huy passages;
* all existing expert ranks;
* H_LOCAL rank if available;
* final LR scores.

Do NOT patch the current experiment from this file.

---

# 29. ANTI-SLOPPINESS TESTS

Mandatory:

1. actual runtime Git HEAD;
2. no hardcoded SHA;
3. exact baseline parity;
4. exact R0 passage parity;
5. no held-fold labels in training;
6. no sibling gold negatives;
7. real PyTorch transformer forward;
8. real backward;
9. optimizer.step count > 0;
10. nonzero gradient norms;
11. model trainable tensors change;
12. checkpoint loads and reproduces logits;
13. no sklearn proxy claimed as Jina adaptation;
14. no QID rules;
15. no document-ID rules;
16. deterministic negative mining;
17. deterministic held scoring;
18. full-data model never reported as OOF;
19. LR train rows use OOF Jina, not in-sample full-data Jina;
20. public full-data Jina uses exactly the locked OOF recipe.

Write:

`INTEGRITY_AUDIT.json`

---

# 30. EXECUTION TRACE

Create:

`EXECUTION_TRACE.jsonl`

For every stage record:

* timestamp;
* command;
* code hash;
* model/checkpoint hash;
* train query count;
* validation query count;
* optimizer steps;
* GPU;
* runtime;
* output hashes;
* status.

A JSON report existing on disk is not proof that computation happened.

---

# 31. DECISION

Compare against:

`0.9488556715777428`

### BREAKTHROUGH

delta >= +0.003

### STRONG_PROMOTE

+0.0015 <= delta < +0.003

### PROMOTE

+0.0007 <= delta < +0.0015

### MARGINAL

0 < delta < +0.0007

### KILL

delta <= 0

Generalization may veto a promotion only for clear catastrophic behavior such as severe unseen-label collapse.

Do not invent manual rescue rules.

---

# 32. REQUIRED OUTPUTS

At minimum:

* `FROZEN_JINA_PARITY.json`
* `NEURAL_TRAINING_PROOF.json`
* `FINETUNE_METHOD_SELECTION.json`
* `TRAIN_SIZE_SCALING_REPORT.json`
* `JINA_OOF_REPORT.json`
* `FINAL_INTEGRATION_REPORT.json`
* `GENERALIZATION_AUDIT.json`
* `TRAIN_DEPLOY_SHIFT_AUDIT.json` if full-data fit executed
* `INTEGRITY_AUDIT.json`
* `EXECUTION_TRACE.jsonl`
* `DECISION.md`

Conditional:

* `FULL6991_TRAINING_MANIFEST.json`
* `JINA_ADAPTATION_CASEBOOK.jsonl`
* new submission candidate + audit/manifest.

---

# 33. FINAL RESPONSE

Report:

1. actual Git HEAD;
2. whether sparse source was committed;
3. baseline parity;
4. actual Jina trainable parameter count;
5. proof that neural weights changed;
6. LoRA vs full-FT inner result;
7. 4.2k vs 5.6k training-size result;
8. frozen Jina R@5;
9. adapted Jina R@5;
10. 3-seed result;
11. J0/J1/J2/J3/J4 final endpoint scores;
12. single/multi deltas;
13. per-fold deltas;
14. wins/losses;
15. unseen-label delta;
16. boundary rescue/damage;
17. selected decision;
18. whether FULL6991 was trained;
19. public candidate path + SHA if created.

If any neural training stage was not genuinely executed, state:

`NEURAL_TRAINING_NOT_EXECUTED`

and do not claim an adapted Jina result.

Execute the experiment now.

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-14T15:31:20+07:00.
</ADDITIONAL_METADATA>