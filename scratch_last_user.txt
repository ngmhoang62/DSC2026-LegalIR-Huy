<USER_REQUEST>
You are executing the next major optimization experiment on folder/repository sota or Huy repo.

This is NOT a planning-only task.

You must inspect the real repository, verify the latest commit and artifacts, implement the experiment, execute every feasible CPU/GPU stage, debug failures, verify integrity, evaluate strict 5-fold OOF, and write all requested artifacts.

Do NOT stop after writing code.

Do NOT merely propose commands for the user to run unless a GPU/resource is genuinely unavailable. If GPU is available, run the experiment.

The priority is:

1. performance;
2. generalization;
3. preserving a deployable/public-test-compatible pipeline.

Scientific elegance is secondary.

However, leakage, local-error hard-coding, QID rules, document-ID rules and held-fold contamination are forbidden because they destroy generalization.

---

# 0. AUTHORITATIVE STARTING POINT

The only authoritative baseline is:

`profile_memory_plus_sparse_rank_scores`

Strict 5-fold OOF:

* Recall@5 = `0.9488556715777428`
* Precision@5 = `0.20314690316120732`
* Single-gold Recall@5 = `0.9641304347826087`
* Multi-gold Recall@5 = `0.7703266787658803`

Per fold:

* fold_0 = `0.9524320457796852`
* fold_1 = `0.9451597520267048`
* fold_2 = `0.9511555873242793`
* fold_3 = `0.9420243204577969`
* fold_4 = `0.9535050071530758`

Current ranking headroom:

* R@6 ≈ `0.955137558`
* R@7 ≈ `0.959178468`
* R@8 = `0.9631359366804939`
* candidate ceiling ≈ `0.981931531`

The current candidate pool remains:

`V2 E5@50 ∪ novel BM25@10`

Do NOT change candidate retrieval in this experiment.

The existing public submission candidate must remain untouched.

---

# 1. NEW NAMESPACE

Create:

`src/gemini/provision_reranker_v1/`

Results:

`results/gemini/provision_reranker_v1/`

Treat all upstream directories as read-only:

* `src/huy_fasttrack/**`
* `results/huy_fasttrack/**`
* `src/gemini/rabr_v1/**`
* `results/gemini/rabr_v1/**`
* `src/gemini/qc_slate_v1/**`
* `results/gemini/qc_slate_v1/**`
* sibling `DSC2026-LegalIR`

You may import their utilities and read their caches.

Do not modify them.

---

# 2. FIRST: AUDIT THE CURRENT REPOSITORY, NOT THE REPORT

Resolve the latest Git commit.

Record:

* commit SHA;
* dirty-tree status if visible;
* exact upstream files imported;
* hashes of all authoritative baseline artifacts.

Recompute baseline predictions from source/cache.

Assert exact parity within `1e-9`.

Write:

`BASELINE_PARITY.json`

Do not proceed if baseline parity fails.

Also verify:

* 6,991 evaluable queries;
* current pool identity;
* folds;
* duplicate-linked exclusions;
* authoritative Top-5 and Top-8 rankings;
* current Jina score cache coverage.

---

# 3. IMPORTANT LEAKAGE LESSON FROM QCSC

The previous QCSC implementation accidentally used globally cross-fitted `P_multi` predictions for outer-training queries.

That meant a training query's `P_multi` could have been fit using labels from the current outer-held fold.

DO NOT repeat this pattern.

For every outer fold F:

* held fold F must be completely invisible to:

  * training;
  * hyperparameter selection;
  * early stopping;
  * auxiliary feature/model construction;
  * negative mining policies that use labels;
  * passage-selection learning;
  * calibration;
  * model selection.

If an auxiliary supervised model is needed for an outer-training query, it must be produced by an inner split that also excludes F.

Add an explicit automated data-flow audit that asserts no held-fold QID enters any supervised fit before held evaluation.

Write:

`FOLD_ISOLATION_AUDIT.json`

---

# 4. HYPOTHESIS

The current cross-encoder may be limited by BOTH:

A. the evidence interface;

and

B. the previous task-adaptation recipe.

Current Huy Jina interface:

* lexical sliding window;
* ~220 whitespace words;
* overlap ~70;
* select only top 2;
* deep windows may prepend ~70-word document header;
* score both passages;
* parent score = max passage score.

Previous Jina adaptation:

* primarily very hard negatives near the top of the frozen-Jina ranking;
* pairwise positive-vs-negative parent-max loss.

We will test whether a better:

1. multi-resolution evidence bank;
2. parent-level multi-instance aggregation;
3. semi-hard negative curriculum;
4. multi-positive group objective;
5. frozen-model stability anchor

can produce a much stronger cross-encoder channel.

The authoritative Huy pipeline remains the fusion backbone.

---

# 5. PHASE A — CHEAP FUSION GEOMETRY RETUNE

Before spending GPU time, test whether legacy Huy LTR geometry is leaving easy performance on the table.

The current pipeline inherited parameters from the old Huy system, especially:

* LogisticRegression `C=0.15`;
* reciprocal-rank feature `1/(10 + rank)`.

Do NOT assume these are optimal after the feature set changed substantially.

## 5.1 Search space

Test only:

`C ∈ {0.01, 0.03, 0.10, 0.15, 0.30, 1.0}`

and reciprocal rank offset:

`k ∈ {2, 5, 10, 20, 40}`

Everything else remains identical:

* StandardScaler;
* balanced LogisticRegression;
* liblinear;
* same 44 feature semantics;
* same rank views;
* same score channels;
* same metadata;
* same candidate pool.

For rank view features replace:

`1/(10 + rank)`

with:

`1/(k + rank)`

consistently for all rank views.

Do NOT change raw-rank features.

## 5.2 Nested evaluation

This must NOT be a naive 30-config OOF leaderboard selection.

For each outer fold:

1. remove outer held fold + duplicate exclusions;
2. perform deterministic inner CV on the remaining queries;
3. select `(C,k)` only from inner results;
4. refit on entire outer-training population with selected `(C,k)`;
5. evaluate outer-held fold once.

Record both:

* exact legacy `(0.15,10)`;
* nested-retuned result.

Also separately report the full 30-config landscape for diagnostics, but DO NOT call the globally best OOF setting an unbiased estimate.

Write:

`FUSION_GEOMETRY_REPORT.json`

If nested retune beats authoritative baseline, define:

`FUSION_ANCHOR`

as the nested OOF endpoint for subsequent diagnostic comparisons.

However, all final reports must still include comparison to original `0.9488556715777428`.

Do not stop the experiment whether fusion tuning wins or loses.

---

# 6. PHASE B — BUILD A LABEL-FREE MULTI-RESOLUTION EVIDENCE BANK

This phase does NOT train a neural model.

For every query, operate on the authoritative current Top-16 parents.

Top-16 is chosen to contain the Top-8 boundary plus enough nearby negatives.

Do not use gold labels to choose evidence passages.

For every `(query,parent)` construct a small evidence bank from multiple deterministic renderers.

## Renderer R0 — HUY_LOCKED

Exact existing Huy:

`top_passages(query, parent, count=2, window=220, overlap=70)`

This is required for parity.

## Renderer R1 — LEXICAL_MEDIUM

Construct ~160-word windows with overlap ~40.

Score windows label-free using query lexical evidence:

* non-stopword token coverage;
* numeric token match;
* bigram/phrase match;
* normalized by length.

Select top 3.

Unlike R0:

* do NOT prepend the entire 70-word header to every deep passage.

Instead attach at most a compact document identity prefix of approximately 24–40 words only if it contains useful title/reference context.

The local evidence must remain the majority of the sequence.

## Renderer R2 — LOCAL_FOCUS

Find high-query-overlap physical sentences/lines or short spans.

Build up to 3 locally coherent windows approximately 80–140 words around the strongest anchors.

Preserve original text order.

Do not construct a bag of disconnected keywords.

## Renderer R3 — STRUCTURAL

Reuse LegalIR `structural_v3` chunks for the parent.

Do NOT blindly take the first structural chunk.

Rank structural chunks label-free for the query using lexical overlap and, if already cached and cheap, existing frozen E5 query/chunk scores.

Keep top 2.

Important:

Prior evidence showed a SINGLE selected structural chunk lost badly to Huy lexical evidence.

Therefore R3 is only one source in a MULTI-RESOLUTION bank.

Do NOT replace all evidence by structural chunks.

## Evidence deduplication

Normalize whitespace/case for comparison.

Remove near-duplicate passages using token Jaccard or another simple deterministic similarity.

Cap final bank at approximately 7 unique passages per `(query,parent)`.

Preserve source labels.

For every passage store:

* qid;
* doc_id;
* renderer;
* passage index;
* text hash;
* token/word length;
* lexical preselection score;
* full text only if storage size is reasonable.

Write:

`EVIDENCE_BANK_MANIFEST.json`

and a cache suitable for neural scoring.

---

# 7. PHASE C — FROZEN CROSS-ENCODER SCREEN

We cannot know in advance whether:

* existing Jina is better;
* BGE reranker is better;
* or one is more complementary with the existing Huy ensemble.

MEASURE IT.

## 7.1 Model A

Mandatory:

`jinaai/jina-reranker-v2-base-multilingual`

Use the exact current frozen checkpoint.

## 7.2 Model B

First inspect the competition/model-approval records already in the repository.

If `BAAI/bge-reranker-v2-m3` is explicitly approved/allowed for this task, also evaluate it.

If it is NOT demonstrably allowed, do not use it and record the reason.

Do not silently assume authorization.

Do not wait for CATI access.

## 7.3 Passage scoring

Score every evidence-bank passage for current Top-16.

Use deterministic inference.

Cache all raw passage logits/scores in SQLite or equivalent restartable format.

Never rerun already hash-matched scores.

For every q-doc derive parent features:

1. `max_score`
2. `second_best_score`
3. `top2_mean`
4. `top3_mean` if >=3 passages
5. temperature-fixed `logsumexp`
6. `max_minus_second`
7. mean score
8. score std
9. number of passages within a fixed small delta of maximum
10. R0/Huy source max
11. R1 source max
12. R2 source max
13. R3 source max

Do not tune dozens of aggregation formulas.

Use these as measurements.

---

# 8. FROZEN EVIDENCE ABLATION

For each frozen CE separately measure:

### A0

Existing Huy R0 + parent max.

This must reproduce current CE rank.

### A1

Full bank + global max.

### A2

Full bank + top2 mean.

### A3

Full bank + fixed-temperature logsumexp.

### A4

Full bank distribution feature block integrated into existing Huy LR:

* retain original CE channel;
* add new parent score/rank;
* add a compact subset of distribution features.

Use strict 5-fold Huy LR.

Do NOT train a neural model yet.

For each arm report:

* standalone CE Recall@5;
* standalone CE Recall@8;
* union oracle with authoritative baseline;
* final Huy-LR Recall@5;
* single/multi;
* fold values;
* wins/losses;
* gold crossings in/out;
* Top-5 churn.

Write:

`FROZEN_EVIDENCE_5FOLD_REPORT.json`

---

# 9. CRITICAL EVIDENCE HEADROOM DIAGNOSTIC

We need to determine whether the evidence bank actually reveals information the current Huy top2 renderer misses.

Across ALL 6,991 queries compute:

For every gold parent inside Top-16:

* old Huy parent score/rank;
* new evidence-bank parent score/rank;
* whether best new-scoring passage came from R0/R1/R2/R3;
* whether the best passage was absent from the old R0 top2;
* movement:

  * > 8 -> <=8
  * 6–8 -> <=5
  * <=5 -> >5
  * etc.

Compare new CE vs old CE on current Top-5 boundary:

* gold rescue crossings;
* gold damage crossings;
* single gold;
* multi gold.

Also compute CLEAN UNION HEADROOM:

`Recall@5 oracle(authoritative baseline Top5, new CE Top5)`

and similar Top8 union.

We are NOT using this oracle in production.

It tells us whether the new CE contains useful complementary information.

Write:

`EVIDENCE_HEADROOM_AUDIT.json`

If the new evidence bank produces almost no additional gold rescue capacity, say so explicitly.

Do NOT fake progress with fusion.

---

# 10. SELECT WHICH CROSS-ENCODER TO ADAPT

The model for neural adaptation must be chosen by measured evidence, not preference.

For Jina and allowed BGE compute a selection score based primarily on:

1. union headroom over authoritative baseline;
2. frozen full-bank Huy-LR gain;
3. standalone discrimination;
4. losses/crossings out;
5. compute feasibility.

Prefer the expert that is **useful and differently wrong**, not merely the highest standalone score.

Write the decision and all numbers to:

`CE_SELECTION.json`

Then adapt exactly ONE primary CE.

If BGE is unauthorized/unavailable, Jina is automatically the primary.

Do not fine-tune two full 5-fold models unless the first selected model fails catastrophically and the other frozen model has clearly better union headroom.

---

# 11. PHASE D — REBUILD TRAINING DATA WITH SEMI-HARD NEGATIVES

This is NOT the previous Jina boundary training recipe.

For each outer fold:

* held outer fold and duplicate-linked exclusions are forbidden;
* use only outer-training labels for training.

Every gold parent is positive.

Never allow sibling golds into negatives.

## 11.1 Negative mining source

Use the AUTHORITATIVE HUY FINAL ranking, not only the selected cross-encoder ranking.

We want negatives that fool the final system.

## 11.2 Primary negative strata

Do NOT use the current Top-5 as routine negatives.

Those are exactly the candidates most likely to be:

* near positives;
* partially relevant;
* ambiguous;
* annotation-borderline.

Use:

### semi-hard

authoritative ranks 6–15:
sample 2 non-gold parents

### medium

ranks 16–30:
sample 2

### tail/easy

ranks 31–50 or remaining pool:
sample 1

If a stratum has insufficient valid negatives, fill deterministically from the next less-hard stratum.

Never insert a gold.

The exact sampling seed must be deterministic.

## 11.3 Optional false-negative guard

Use the frozen CE ensemble ONLY as a label-free guard.

If a supposed negative is simultaneously extremely high-confidence under:

* old Jina;
* new evidence CE;
* adapted E5/LAL consensus;

do NOT automatically turn it positive.

Instead move it out of the primary negative pool and sample another negative.

Record count of guarded candidates.

This is a false-negative avoidance mechanism, not pseudo-labeling.

Do not use arbitrary hand-written thresholds selected on held labels.

Use a fixed percentile derived only from outer-training score distributions.

---

# 12. POSITIVE EVIDENCE AS MULTI-INSTANCE BAG

Do NOT predeclare one passage as the true positive passage.

Gold labels are parent-level.

Each parent is a bag of evidence passages.

For a parent with passage logits:

`s1 ... sm`

construct differentiable parent score using a fixed soft evidence pooling.

Primary:

`parent_score = logsumexp(scores / tau) * tau`

Use ONE reasonable fixed tau such as `0.15` or another scale appropriate after examining score ranges on OUTER TRAINING ONLY.

Do not grid-search tau on outer held data.

Also preserve max-score diagnostics.

This allows the model to learn from a useful provision without falsely labelling every passage in a gold document as independently positive.

---

# 13. TRAINING OBJECTIVE — MULTI-POSITIVE LISTWISE

Previous pairwise max-vs-max training is not the primary objective.

For query q with positive parents P and sampled candidate parents C:

Use a query-group multi-positive softmax objective:

`L_rank(q) = -log( sum_{p in P} exp(s_p / T) / sum_{d in C} exp(s_d / T) )`

where:

* all gold parents available to the training query are positives;
* negatives are semi-hard/medium/easy mined parents;
* every query contributes equal weight.

This naturally supports multi-gold queries.

Use fixed T.

Do not make T a large tuning grid.

---

# 14. STABILITY / DISTILLATION ANCHOR

The previous task adaptation may have catastrophically overwritten useful frozen cross-encoder behavior.

Prevent this.

Before training, cache frozen parent scores for every training group.

Add:

`L_anchor = MSE(adapted_parent_scores, frozen_parent_scores)`

after a reasonable affine normalization if the raw scale requires it.

Total:

`L = L_rank + lambda_anchor * L_anchor`

Primary lambda choices:

`{0.15, 0.30}`

Do not use a large sweep.

The goal is:

learn legal discrimination while retaining the strong generic frozen geometry.

---

# 15. TRAINING MECHANICS

If selected model is Jina, reuse the already-debugged LoRA infrastructure from the prior Jina experiment where possible.

Do NOT reuse its negative mining or pairwise scientific recipe.

For Jina:

* use conservative LoRA;
* target previously validated compatible attention modules;
* do not introduce new architecture bugs;
* low learning rate.

Evaluate only:

`lr ∈ {5e-6, 1e-5}`

and:

`lambda_anchor ∈ {0.15, 0.30}`

4 configs maximum.

If BGE is selected, use an equivalent parameter-efficient or full fine-tune compatible with available memory.

Do a smoke run first.

For every outer fold:

1. outer held + duplicate-linked exclusions removed;
2. create deterministic inner train/validation split from remaining data;
3. select lr/lambda only on inner validation;
4. checkpoint by inner Recall@5 / ranking utility;
5. refit or use selected checkpoint according to a fixed policy;
6. evaluate outer held ONCE.

No global OOF auxiliary prediction may leak outer-held labels into outer training.

---

# 16. EARLY STOPPING METRIC

Do not stop only on pair accuracy.

On inner validation, score the actual candidate groups and prioritize:

1. parent Recall@5;
2. wins-losses around Top-5;
3. multi-gold Recall@5;
4. frozen-score retention.

Pair accuracy is diagnostic only.

---

# 17. ADAPTED CE SCORING

For held queries score current authoritative Top-16 using the SAME evidence bank contract used in training.

Do not change evidence selection after training.

Cache passage and parent scores.

Produce:

* adapted CE standalone ranking;
* frozen CE ranking;
* delta and crossings.

Write per-fold immutable prediction files with hashes.

---

# 18. INTEGRATION INTO THE AUTHORITATIVE HUY PIPELINE

Evaluate ALL of these, because stronger standalone CE may still be redundant.

### B0

Exact authoritative baseline.

### B1 — CE_REPLACE

Replace existing frozen Huy Jina score/rank with adapted CE score/rank.

Keep every other feature identical.

### B2 — CE_AUGMENT

Keep frozen Jina channel AND add adapted CE rank/score.

This tests complementarity.

### B3 — CE_MULTIINSTANCE_AUGMENT

B2 plus only the compact parent evidence-distribution features that existed BEFORE looking at held results.

No manually discovered boundary rules.

### B4 — BEST_WITH_FUSION_GEOMETRY

If Phase A nested fusion retune passed, combine the best CE integration with that fusion geometry.

All integrations are strict 5-fold.

No post-hoc slot-5 swapper.

No query-specific gating.

---

# 19. OPTIONAL RESIDUAL FEATURE — ONLY IF JUSTIFIED

If adapted CE improves standalone substantially but B1/B2 fail to improve final Huy LR because it is correlated with the frozen CE, add exactly two residual features:

* `adapted_score_z - frozen_score_z`
* `adapted_rank - frozen_rank`

Then evaluate one arm:

`CE_RESIDUAL_AUGMENT`

This tests whether the adaptation delta itself contains useful information.

Do NOT launch another feature zoo.

---

# 20. GENERALIZATION AUDIT

For every serious endpoint report:

* Recall@5;
* Precision@5;
* single-gold;
* multi-gold;
* per-fold;
* worst fold delta;
* wins/losses/ties;
* crossings into/out of Top-5;
* Top5 churn.

Also slice by:

### document length

* short
* medium
* long

Use training-derived quantiles, not manual thresholds.

### source of best evidence

* R0
* R1
* R2
* R3

### promoted original rank

* 6
* 7–8
* 9–16

### frozen CE agreement

* frozen and adapted agree;
* adapted flips frozen.

### negative hardness

For held errors, which training hardness regime most resembles them based on score/rank statistics.

### single vs multi gold

### seen vs unseen gold document

Whether candidate document ever appears as a gold label in outer training.

A good new expert must not derive essentially all gains from documents repeatedly seen as labels during training.

Write:

`GENERALIZATION_AUDIT.json`

---

# 21. ERROR ANALYSIS ARTIFACT — CONDITIONAL

Do NOT use error analysis to make manual rules during this experiment.

However, if the final best delta is below `+0.002`, create:

`PROVISION_RERANKER_CASEBOOK.jsonl`

Include deterministic cases:

* up to 25 true rescues;
* up to 25 new harms;
* up to 25 remaining Top-8 oracle opportunities where new CE still misses.

For each case include:

* qid;
* question;
* gold IDs;
* authoritative Top-8;
* old Top-5;
* new Top-5;
* old LR scores;
* frozen CE scores;
* adapted CE scores;
* all expert ranks;
* defender and challenger titles/references;
* evidence passages used by R0/R1/R2/R3;
* per-passage frozen/adapted scores;
* best evidence source;
* document lengths.

This file is only for the NEXT research decision.

Do not use it to patch the current experiment.

If final delta >= +0.002 and generalization is healthy, this casebook is optional.

---

# 22. ANTI-SLOPPINESS / INTEGRITY TESTS

Add automated tests for all of the following:

* exact baseline parity;
* no held-fold QID in any supervised outer-training fit;
* no held-fold label in:

  * hyperparameter tuning;
  * auxiliary models;
  * negative mining;
  * passage selection;
  * early stopping;
* duplicate-linked exclusions respected;
* sibling gold never mined as negative;
* evidence rendering independent of labels;
* deterministic evidence bank hashes;
* deterministic negative sampling;
* score cache integrity;
* passage text hash parity;
* current R0 Jina scores reproduce frozen cache within numeric tolerance;
* no production QID or canonical document ID hard-coded;
* no import from RABR/QCSC forensic QID lists into production decisions;
* exactly one fixed inference recipe per selected endpoint;
* same renderer at train and inference;
* deterministic held predictions on rerun.

Scan the entire new namespace recursively for suspicious document/QID literals and report them.

Write:

`INTEGRITY_AUDIT.json`

---

# 23. GPU / COMPUTE POLICY

Do NOT kill a promising direction merely because it is computationally expensive.

Use caching aggressively.

If one GPU is available, run sequentially.

If multiple GPUs are available, parallelize folds safely.

Never change scientific computation just to fit runtime unless the change is explicitly recorded and numerically validated.

If OOM:

1. reduce microbatch;
2. use gradient accumulation;
3. gradient checkpointing;
4. fp16/bf16;
5. cache passage tokenization if practical.

Do NOT reduce the training population first.

---

# 24. DECISION RULES

The primary comparator remains:

`0.9488556715777428`

Classify the best final endpoint:

### BREAKTHROUGH

`delta >= +0.003`

### STRONG_PROMOTE

`+0.0015 <= delta < +0.003`
and:

* wins > losses;
* no fold worse than `-0.0015`;
* no obvious memorization-only gain.

### PROMOTE

`+0.0007 <= delta < +0.0015`
with healthy generalization.

### MARGINAL

`0 < delta < +0.0007`

### KILL

`delta <= 0`

Do NOT alter a result with manual query rules to cross a gate.

---

# 25. PUBLIC MATERIALIZATION

If an endpoint is PROMOTE or better:

Fit the public/deployment pipeline using all 6,991 evaluable training queries.

For hyperparameters:

* use the selection procedure justified by training CV;
* never use public leaderboard labels/scores to tune.

Score the 1,000 public queries with exactly the same evidence/reranker contract.

Create a NEW submission candidate.

DO NOT UPLOAD IT.

Audit:

* exactly 1,000 queries;
* exactly 5 unique parent IDs/query;
* every ID canonical;
* ZIP contains only correct `submission.json`;
* byte parity;
* model parameter budget <4B original parameters;
* manifest hashes.

Preserve all previous submissions unchanged.

---

# 26. REQUIRED ARTIFACTS

At minimum produce:

`BASELINE_PARITY.json`

`FOLD_ISOLATION_AUDIT.json`

`FUSION_GEOMETRY_REPORT.json`

`EVIDENCE_BANK_MANIFEST.json`

`FROZEN_EVIDENCE_5FOLD_REPORT.json`

`EVIDENCE_HEADROOM_AUDIT.json`

`CE_SELECTION.json`

`NEGATIVE_MINING_AUDIT.json`

per-fold training manifests

per-fold checkpoint hashes

`ADAPTED_CE_5FOLD_REPORT.json`

`FINAL_COMPARISON.json`

`GENERALIZATION_AUDIT.json`

`INTEGRITY_AUDIT.json`

`DECISION.md`

Conditional:

`PROVISION_RERANKER_CASEBOOK.jsonl`

If promoted:

`submission_candidate/submission.zip`

`submission_candidate/MANIFEST.json`

`submission_candidate/FINAL_AUDIT.json`

---

# 27. DO NOT BE LAZY ABOUT INTERMEDIATE RESULTS

At every phase actually inspect the output before proceeding.

Examples:

After fusion geometry:
determine whether legacy C/k were leaving performance unused.

After frozen evidence:
determine which renderer actually contributes useful passages.

After frozen CE screen:
choose model using complementarity/headroom, not name preference.

After negative mining:
print hardness statistics and verify gold exclusion.

After Fold-0 training:
compare:

* frozen CE;
* adapted CE;
* integrated Huy endpoint.

If Fold-0 is catastrophically worse (`< -0.005` final Huy delta) AND adapted CE itself clearly collapses, debug training before wasting four more folds.

Do not immediately KILL after one bad Fold-0 until you have checked:

* optimizer/loss correctness;
* passage parity;
* negative hardness distribution;
* frozen-score anchor magnitude;
* score scale;
* checkpoint loading.

If implementation is correct and Fold-0 remains clearly catastrophic, then kill neural adaptation but still retain any frozen-evidence or fusion improvements already found.

---

# 28. FINAL RESPONSE FORMAT

Keep the final response concise but complete.

Report in this order:

1. latest commit / integrity status;
2. baseline parity;
3. best nested fusion geometry result;
4. frozen evidence results:

   * Jina;
   * BGE if legal;
5. selected CE and why;
6. adapted CE standalone delta;
7. final Huy endpoint delta;
8. single/multi deltas;
9. per-fold deltas;
10. wins/losses;
11. crossings in/out;
12. evidence source statistics;
13. generalization verdict;
14. decision;
15. artifact paths;
16. if submission candidate exists:

* path;
* SHA256;
* audit status.

Do not claim success merely because the neural expert improves standalone.

The final Huy-fasttrack Recall@5 is the primary objective.

Execute the full experiment now.

</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-09-14T11:25:23+07:00.
</ADDITIONAL_METADATA>